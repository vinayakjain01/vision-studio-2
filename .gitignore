# dependencies
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# production
/build

# runtime data — SQLite database and all media (originals, derived, creatives)
/data/

# ONNX model weights — fetched by `npm run models:fetch`, never committed
/models/*
!/models/.gitkeep

# env
.env
.env*.local

# misc
.DS_Store
*.pem
*.tsbuildinfo

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
